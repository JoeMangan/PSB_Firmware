/*
 * i2c.h
 *
 *  Created on: Aug 12, 2023
 *      Author: jm
 */

#ifndef I2C_H_
#define I2C_H_


/*
//#include "main.h"

#define I2C_TIMEOUT_DURATION 10 // Not sure of the units of this - could be ms
#define I2C_TX_ATTEMPT_PERIOD 100 // ms
#define I2C_TX_MAX_ATTEMPTS 5
#define I2C_RX_ATTEMPT_PERIOD 100 // ms
#define I2C_RX_MAX_ATTEMPTS 5

// Variables
HAL_StatusTypeDef i2c_TX_status;		// Status of the TX operation
HAL_StatusTypeDef i2c_RX_status;		// Status of the RX operation
// TIMEOUT DURATION
// MAX ATTAMPTS

// Functions
HAL_StatusTypeDef i2c_write(uint8_t dev_addr, uint8_t *out_ptr, uint16_t countTX);
HAL_StatusTypeDef i2c_read(uint8_t dev_addr, uint8_t *in_ptr, uint16_t countRX);
HAL_StatusTypeDef i2c_write_read(uint8_t dev_addr, uint8_t *out_ptr, uint16_t countTX, uint8_t *in_ptr, uint16_t countRX);
*/


#endif /* I2C_H_ */
